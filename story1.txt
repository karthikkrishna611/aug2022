git space clone ("TO DOWNLOADE THE REPO FOR THE FIRST TIME")
git status("TO know the status of Branch OR changes in the file")
git checkout ("TO change the current working branch")
remote("SERVER OR REPO)
local file sysytem and local repo

stage
selecting the file
commit with msg
push local to remote
pull getting updated changes from remote to both local and local file 

first step ->staging
git add . or git add filename -> if it show untracked files error -> staging
git commit -m= "Message"-> TO commit will move the changes from local
l to local repo.
git push origin branchname -> it take the changes from  Local to remote 

merge -> combining the changes from 2 branches , story branch to dev branch

git merge origin/dev origin/story1

git checkout branch_name 

switch branch: